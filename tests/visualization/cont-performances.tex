\begin{tikzpicture} 
    \begin{loglogaxis}[mark repeat= {1},
                         width=12cm, height=10cm,
                         cycle list name=color list,
                         ylabel = {\texttt{walltime} [s]},
                         xlabel = {$\frac{1}{2}\|\hat x - x_0 \|_{L^2}^2$},
                         grid=major,
                         legend style = {legend cell align=left, legend pos = north east,
                         font =\footnotesize},
                         line width = 1pt,
                         ]
        \addplot[color=blue,
                 only marks,
                 mark=*,
                 mark options={scale=2, fill=blue}
                 ] table [y=trc, x=l] {../mh-data/cw60stbl.dat};
        \addplot[color=green, 
                 only marks, 
                 mark=*, 
                 mark options={scale=2, fill=white}
                 ] table [y=trc, x=l] {../mh-data/cw60ntbl.dat};
        %       \legend{ \normalsize{$\hat K \in \{24, ..., 96\}$ },
        %                \normalsize{$(\hat q, n_t) \in \{(13,18), ..., (18,13)\}$}       
        %        }
     \end{semilogyaxis}
\end{tikzpicture}
